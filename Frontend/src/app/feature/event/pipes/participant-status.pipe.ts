import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'participantStatus',
  standalone: true
})
export class ParticipantStatusPipe implements PipeTransform {
  // TODO: Implement participant status pipe
} 