import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'availabilityFormat',
  standalone: true
})
export class AvailabilityFormatPipe implements PipeTransform {
  // TODO: Implement availability format pipe
} 